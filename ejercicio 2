# Lista de códigos de productos en el inventario (arreglo)
codigos_productos = ["A001", "A002", "B001", "C001"]

# Definir un código de producto para prueba (en lugar de usar input())
codigo = "A001"  # Cambia este valor para probar diferentes códigos

# Verificar si el código está en la lista
if codigo in codigos_productos:
    # Obtener la posición del código
    posicion = codigos_productos.index(codigo)  
    print(f"El código {codigo} se encuentra en la posición {posicion}.")
else:
    print("El código no se ha encontrado en el inventario.")
